spring:
  cloud:
    nacos:
      config:
        server-addr: "127.0.0.1:8848"
        file-extension: yaml
        timeout: 5000
        #config-long-poll-timeout:
        #group:
  application:
    name: nacos-config
server:
  port: 8766



